{
  "name": "chapter_4",
  "version": "1.0.0",
  "description": "A dockerized fullstack todo application that uses a NodeJS backend, a PostgressSQL database, a Prisma ORM and JWT Authentication.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file=.env --experimental-strip-types --experimental-sqlite ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.4.0",
    "bcryptjs": "^3.0.0",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.3",
    "prisma": "^6.4.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
